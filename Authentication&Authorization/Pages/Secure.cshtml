@page
@model Authentication_Authorization.Pages.SecureModel
@{
}

<div>
    <h3>User Claims</h3>
    <dl>
        @foreach(var claim in User.Claims)
        {
            <dt>@claim.Type</dt>
            <dd>@claim.Value</dd>
        }
    </dl>

</div>
